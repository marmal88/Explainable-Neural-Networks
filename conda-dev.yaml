name: xnn
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - pylint
      - pytest-cov
      - black
      - ipykernel
      - tqdm
      - pre-commit
      - hydra-core
      - numpy
      - pandas
      - torch
      - torchvision
      - lightning[extra]
      - albumentations
      - jsonargparse[signatures]

